 **简答**

**定调**：TDD（测试驱动开发）是一种**先写测试，再写代码**的软件开发方法，用测试来定义需求、驱动设计、保障重构。

**比喻**：就像**先立好靶子，再射箭**。  
传统开发是“先射箭，再画靶”（先写代码，再补测试），而TDD是“靶子立好，才允许拉弓”——你先用测试把期望的行为钉死，然后编码去命中它。  
*失真提醒：这个比喻把测试比作“靶心”，但实际测试用例是不断增加的，不是单一靶子；且靶子不会像自动化测试一样反复检验后续改动。*

**收束**：TDD的核心循环是 **红 → 绿 → 重构**：先写一个会失败的测试（红），再用最少代码让测试通过（绿），然后优化代码结构（重构），以此形成安全网，让代码始终可验证、可演进。